//program to find the volume of a sphere
# include<stdio.h>
#define pi 3.142
int main ()
{
    int r=4;
    int volume;
    volume=4/3*pi*r*r*r;
    printf("to find the volume of a sphere=%d metres\n", volume);
    return 0;
}
