//program that accept two intergers from user and calculate the product of the two intergers
#include<stdio.h>
int main()
{
    int m,n,p;
    printf("\n enter the first number;");
    scanf("%d",&m);
    printf("enter the second number:");
    scanf("%d",&n);
    p=m*n;
    printf("the product is=%d\n",p);
    
}